{
    "configurations": [
        {
            "name": "Current streamlit file",
            "type": "python",
            "request": "launch",
            "module": "streamlit",
            "args": ["run", "${file}"],
            "cwd": "${workspaceFolder}/ai_text_detection",
            "env": {
                // "PYTHONPATH": "${workspaceFolder}/...[**potential subdirectories**].../${pathSeparator}${env:PYTHONPATH}"
                "PYTHONPATH": "${workspaceFolder}/ai_text_detection:${env:PYTHONPATH}"

            }
        }
    ]
}